# Docker-specific environment variables
# This file contains Docker container paths - copy to .env.docker for Docker usage

# Controller
ORCHESTRY_DB_PATH=/app/data/orchestry.db

# Nginx 
ORCHESTRY_NGINX_CONTAINER=orchestry-nginx
ORCHESTRY_NGINX_CONF_DIR=/nginx-config

# Controller cluster settings (Load balancer port)
ORCHESTRY_HOST=0.0.0.0
ORCHESTRY_PORT=8000

# Controller Load Balancer Settings
CONTROLLER_LB_HOST=controller-lb
CONTROLLER_LB_PORT=8000

# Individual Controller Ports (for internal cluster communication)
CONTROLLER_1_PORT=8001
CONTROLLER_2_PORT=8002
CONTROLLER_3_PORT=8003

# Optional
# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Docker socket path (if different from default)
# DOCKER_SOCKET=/var/run/docker.sock

# Network name for containers
# ORCHESTRY_NETWORK=orchestry

# SSL/TLS Configuration (if using HTTPS)
# ORCHESTRY_SSL_CERT_PATH=/path/to/cert.pem
# ORCHESTRY_SSL_KEY_PATH=/path/to/key.pem

# Metrics configuration
# ORCHESTRY_METRICS_ENABLED=true
# ORCHESTRY_METRICS_PORT=9090

# Health check configuration
# ORCHESTRY_HEALTH_CHECK_INTERVAL=30
# ORCHESTRY_HEALTH_CHECK_TIMEOUT=10

# Scaling configuration defaults
# ORCHESTRY_DEFAULT_MIN_REPLICAS=1
# ORCHESTRY_DEFAULT_MAX_REPLICAS=10
# ORCHESTRY_DEFAULT_TARGET_CPU=70
# ORCHESTRY_DEFAULT_TARGET_MEMORY=80